# 戦闘入力 検証版 20260923-1

## 導入

ゲームを終了し、このZIPを展開して install.bat を実行。DLL入りなのでビルド不要。
既存の同名MODとログは Mods の隣の DonChanModBackups に退避されます。
手動コピーで上書きしないでください。別のModsフォルダーを使う場合は
`install.bat -ModsRoot "実際のModsフォルダー"` を使用。
同名MODをゲーム側ModsとAppData側Modsの両方に置かないでください。
EACを無効にしたソロ環境で起動。今回はAITuberを起動しなくても収集できます。
レーダーDLLはこのパッケージに含みません。既存レーダーの表示は比較用です。

## マーカー

プレイヤー操作中に **F9** を1回押すごとに次の番号へ進みます。押してから表の操作を実施。
**F10** は同じ番号に再試行マーカーを追加。ゲーム再起動で番号は0に戻ります。
F1のゲームコンソールに `VALIDATION STEP 番号 / ラベル` が記録されます。
表示されなければ続けず、ログを送ってください。F9/F10に他MODの機能を割り当てている場合は競合を避けてください。
各操作の間は3秒ほど待つ。一度に1体で実施し、必要なら新しい個体を使用。
判定の正誤はログ解析で確認するため、この時点で頭判定・撃破帰属が成功したとはみなしません。

|F9回数|操作|
|---|---|
|1|周囲に対象を置かず5秒待つ。基準ログ。|
|2|人型ゾンビ1体を15m内で生存のまま5秒観察→倒す→死体の近くで5秒待つ。|
|3|ゾンビ犬で同じ操作。|
|4|ゾンビクマで同じ操作。|
|5|ハゲワシで同じ操作。変種も試せるならF10で区切る。|
|6|ダイアウルフで同じ操作。|
|7|グレースで同じ操作。通常イノシシと取り違えない。|
|8|近接A（例：棍棒）で通常ゾンビの胴体に単発2回。|
|9|同じ武器で頭を狙って単発2回。|
|10|近接B（例：ナイフ）で胴体に単発2回。出血などが起きたらメモ。|
|11|同じ武器で頭を狙って単発2回。|
|12|銃1種で胴体に単発2回。最初は散弾・連射を避ける。|
|13|同じ銃で頭を狙って単発2回。|
|14|水平距離25m以上の未警戒の敵を撃つ→倒す。接近して死体を確認。射撃時に15m内へ入れない。|
|15|水平15m以内で高低差のある生存対象を観察。可能なら未警戒→プレイヤーを狙う→離脱の順で、切替時にF10。通常動物の混入確認もF10で区切って任意追加。|
|16|終了マーカー。ゲームを終了する。|

途中で無理な項目は飛ばして次のF9へ。飛ばした番号・使用武器・狙った部位・意図しない死亡を短くメモしてください。
「頭を狙った」は実際に頭へ当たったことの証明ではないので、可能なら短い動画もあると比較しやすいです。
敵を準備する時間を除き15〜20分程度が目安。全項目を一度に完了する必要はありません。

## ログ提出

ゲーム終了後、展開フォルダーの **export-logs.bat** を実行。
同じ場所にできる `combat-validation-logs-日時-識別子.zip` と簡単なメモをチャットへ添付。
カスタム導入先の場合は export-logs.bat にも同じ -ModsRoot を指定。
元ログは `%APPDATA%\7DaysToDie\Mods\DonChanTelemetryProbe\Telemetry`。
ZIPには events.jsonl / snapshots.jsonl / capabilities.jsonl / session.json / validation.json などが入ります。
起動しない場合はゲームの起動ログも必要です。

## 確認対象と限界

- 6群のクラス名・分類、生存/死亡、水平15m判定、高低差無視。
- attackTarget と attackTargetClient の値を別々に記録。現在の targetState だけで戦闘確定とはしません。
- 距離・敵対で切らないcombatフック、命中部位、ダメージ源、死亡イベントの対応。
- validation.combat は名前付き引数と明示的な部位・死亡応答Sourceを収録。test.markerと各レコードのvalidationStepで照合。
- DamageEntityの入力amountは実際のHP減少量とは限りません。Killの呼び出しはプレイヤー撃破確定ではありません。
- 未取得・例外は確定したゼロとして解釈しません。欠落や対象参照の残存は実機で確認します。
- 提供済みゲームDLLに対しC#5でビルド成功。現在のゲームでのロード・実戦動作は未検証。
- validation.jsonに実行時のAssembly-CSharp/Harmony/検証DLLの識別子と部位enumを保存。
- 通常運用へ戻す場合は設定XMLの EnableCombatValidation をfalse、SnapshotIntervalSecondsを1.0へ変更して再起動。
- リポジトリ標準設定は検証false。配布ZIPだけtrue/0.5秒に設定。
